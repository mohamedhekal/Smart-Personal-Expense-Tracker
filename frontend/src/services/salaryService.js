import { t } from './languageService';

/**
 * Check and add recurring monthly salaries automatically
 */
export const checkAndAddRecurringSalaries = async () => {
    // Server handles recurring; no-op
    return;
};

/**
 * Initialize salary check service
 * Runs check on app load and sets up interval for daily checks
 */
export const initSalaryService = () => {
    // No-op
};
