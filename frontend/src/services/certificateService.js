import { t } from './languageService';

/**
 * Check and add recurring monthly certificate returns automatically
 */
export const checkAndAddCertificateReturns = async () => {
    // Server handles certificate returns; no-op
    return;
};

/**
 * Initialize certificate return check service
 * Runs check on app load and sets up interval for daily checks
 */
export const initCertificateService = () => {
    // No-op
};

